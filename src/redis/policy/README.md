# Policy modules

Iteration 6 keeps choose/swap rules in `../adaptive.aura` / `../adaptive_body.aura`
(and inlined in `../server_ffi.aura` for the live serve_ms supervisor).

Future: named bodies for `std/hot-strategy`, or Iteration 7 reloadable eviction
`.so` packs via `std/hot-update`.
